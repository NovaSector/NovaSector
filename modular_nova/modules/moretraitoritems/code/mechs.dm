/obj/vehicle/sealed/mecha/ripley/deathripley/normal_clamp // tg's options are either a 0 dmg clamp or a giga-op clamp, so here's a normal 20 dmg clamp
	equip_by_category = list(
		MECHA_L_ARM = /obj/item/mecha_parts/mecha_equipment/hydraulic_clamp,
		MECHA_R_ARM = null,
		MECHA_UTILITY = list(/obj/item/mecha_parts/mecha_equipment/ejector),
		MECHA_POWER = list(),
		MECHA_ARMOR = list(),
	)
