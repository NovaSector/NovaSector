/// Mood category used for the dominance/submission moodlet bucket.
#define DOMINANT_MOOD "dominant_mood"

/// Per-mob throttle: how often a dom's finger-snap can actually drop the nearest subs.
#define DOMINANT_COOLDOWN_SNAP "dominant_cooldown_snap"
/// Per-mob throttle: how often a sub flustering at a dom re-fires the "they look away" flavor.
#define DOMINANT_COOLDOWN_EXAMINE "dominant_cooldown_examine"
/// Per-mob throttle on a sub's "someone's presence makes you submissive" chat spam.
#define DOMINANT_COOLDOWN_NOTICE "dominant_cooldown_notice"
